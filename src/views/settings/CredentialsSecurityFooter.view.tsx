// 凭证页页底安全承诺（产品 21-3 §3）。纯展示、零 props 决策、零副作用。
//
// ⛔ **这段话此前少了一半，而且埋错了地方。**
//    产品 §3 要求页底写两件事：① 凭证加密保存在本机、不上传；
//    ② **「帐号授权在您自己的浏览器中完成，平台不接触您的密码」**。
//    实现里只留了 ①，② 整条消失 —— 而 ② 才是回答「我把 GitHub / 模型帐号密码交给这个平台了吗」
//    的那一句，也是前端设计文档验收表 #23 点名说过「删掉不会有人发现」的那一条。它确实被删了，
//    也确实没人发现。
//
//    ③ 位置同样是缺陷的一部分：原文在 **Git 分区内部**的 `<footer>` 里，于是这两句承诺
//    只对 Git 那半边成立，Agent 帐号区对「我的模型帐号存在哪」一个字都没说。
//    ⇒ 现在它跨两个分区，落在页底。
//
// ⚠️ 改这里之前先读一遍产品 21-3 §3：这三句是**合规承诺**，不是装饰性说明，
//    删任何一句都要有产品裁决。

export function CredentialsSecurityFooterView() {
  return (
    <footer
      data-testid="credentials-security-footer"
      className="mt-2 flex flex-col gap-1 border-t border-border pt-3 text-xs text-muted-foreground"
    >
      <p>凭证加密保存在这台机器上，不会上传；平台也读不回明文。</p>
      <p>帐号登录在你自己的浏览器里完成，平台不接触你的密码。</p>
      <p>SSH 私钥与 Token 存进去之后就不再显示，只留指纹 / 尾号用来认人。</p>
    </footer>
  );
}
